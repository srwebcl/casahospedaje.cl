---

---

<section id="nosotros" class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center max-w-3xl mx-auto">
            <h2
                class="text-3xl md:text-4xl font-serif font-bold text-[#1e3a8a] mb-8 reveal"
            >
                Nuestra Esencia
            </h2>
            <div
                class="prose prose-lg mx-auto text-gray-600 reveal reveal-delay-200"
            >
                <p class="leading-relaxed text-lg mb-12">
                    Más que un hotel, somos una <strong
                        >casa tradicional porteña</strong
                    > atendida por sus propios dueños. Ofrecemos un ambiente sencillo,
                    familiar y tranquilo. Si buscas la experiencia auténtica de vivir
                    en un cerro de Valparaíso, con una vista privilegiada y sin pagar
                    de más, este es tu lugar. Nuestra infraestructura es clásica,
                    pero nuestra atención es <span
                        class="text-[#d97706] font-semibold">24/7</span
                    >.
                </p>
            </div>
        </div>

        <!-- Gallery Carousel (Wider Layout) -->
        <div class="relative w-full max-w-6xl mx-auto mt-12 group/gallery">
            <!-- Navigation Buttons -->
            <button
                id="gallery-prev"
                class="absolute left-4 top-1/2 -translate-y-1/2 z-20 bg-white/90 hover:bg-[#d97706] hover:text-white text-[#1e3a8a] p-3 rounded-full shadow-lg backdrop-blur-sm transition-all duration-300 opacity-0 group-hover/gallery:opacity-100 hidden md:flex items-center justify-center transform hover:scale-110"
                aria-label="Anterior"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-6 h-6"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><path d="m15 18-6-6 6-6"></path></svg
                >
            </button>

            <button
                id="gallery-next"
                class="absolute right-4 top-1/2 -translate-y-1/2 z-20 bg-white/90 hover:bg-[#d97706] hover:text-white text-[#1e3a8a] p-3 rounded-full shadow-lg backdrop-blur-sm transition-all duration-300 opacity-0 group-hover/gallery:opacity-100 hidden md:flex items-center justify-center transform hover:scale-110"
                aria-label="Siguiente"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-6 h-6"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"><path d="m9 18 6-6-6-6"></path></svg
                >
            </button>

            <div
                id="gallery-container"
                class="flex overflow-x-auto gap-6 pb-8 snap-x snap-mandatory scrollbar-hide px-4 md:px-0"
            >
                {
                    [
                        "/imagenes/hospedaje-valparaiso-56.jpeg",
                        "/imagenes/hospedaje-valparaiso-51.jpeg",
                        "/imagenes/hospedaje-valparaiso-45.jpeg",
                        "/imagenes/hospedaje-valparaiso-44.jpeg",
                        "/imagenes/hospedaje-valparaiso-43.jpeg",
                        "/imagenes/hospedaje-valparaiso-37.jpeg",
                        "/imagenes/interior.jpeg",
                    ].map((src, index) => (
                        <div class="relative flex-shrink-0 w-[85%] md:w-[60%] snap-center group">
                            <div class="relative aspect-[16/10] overflow-hidden rounded-2xl shadow-xl">
                                <img
                                    src={src}
                                    alt={`Vista Casa Hospedaje ${index + 1}`}
                                    class="w-full h-full object-cover transform transition-transform duration-700 group-hover:scale-105"
                                    loading="lazy"
                                    decoding="async"
                                    width="1000"
                                    height="625"
                                />
                                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 pointer-events-none" />
                            </div>
                        </div>
                    ))
                }
            </div>
        </div>

        <script>
            const container = document.getElementById("gallery-container");
            const prevBtn = document.getElementById("gallery-prev");
            const nextBtn = document.getElementById("gallery-next");

            if (container && prevBtn && nextBtn) {
                const scrollAmount = () => container.offsetWidth * 0.6; // Scroll one slide width

                nextBtn.addEventListener("click", () => {
                    container.scrollBy({
                        left: scrollAmount(),
                        behavior: "smooth",
                    });
                });

                prevBtn.addEventListener("click", () => {
                    container.scrollBy({
                        left: -scrollAmount(),
                        behavior: "smooth",
                    });
                });
            }
        </script>
    </div>
</section>
